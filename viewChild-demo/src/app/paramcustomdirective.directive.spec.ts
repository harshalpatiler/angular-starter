import { ParamcustomdirectiveDirective } from './paramcustomdirective.directive';

describe('ParamcustomdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ParamcustomdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
